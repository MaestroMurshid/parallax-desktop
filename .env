# UI-mockup phase: use the fixture backend even inside Tauri.
# Delete this line once the Rust commands in src-tauri/src/commands land.
# Deleted: the Rust command layer exists now. Set it back to force the fixtures.
# NEXT_PUBLIC_BRIDGE=mock

# Replays onboarding on every launch, for working on it. Unset, onboarding
# shows until every chosen model is on disk.
# NEXT_PUBLIC_ALWAYS_ONBOARD=1
